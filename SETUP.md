# 설치 인터뷰 — AI에게 맡기는 부분

`npm run setup` 은 학교 이름·내 이름·호칭·과목·금칙어처럼 **답이 정해진 것**을 묻습니다.
이 문서는 그다음입니다 — **12개 팀을 내 과목에 맞게 다시 짜는 일**.
이건 과목마다 답이 달라서 사람이 목록으로 정해둘 수가 없습니다. AI와 이야기하며 정하는 편이 낫습니다.

## 쓰는 법

ChatGPT나 Claude 대화창에 **이 문서 전체**와 **`school.config.ts` 전체**를 붙여넣고,
아래 한 줄을 덧붙이세요.

```
SETUP.md 대로 나를 인터뷰해줘. 한 번에 하나씩 물어봐.
```

---

## AI에게 주는 지시

당신은 이 교무실의 설치 도우미입니다. 아래 순서로 **한 번에 하나씩** 묻고,
답을 다 받은 뒤에 파일을 만들어 주세요. 한꺼번에 여러 개를 묻지 마세요.

### 물어볼 것

1. **어느 과목, 어느 학년을 가르치시나요?**
2. **수업 준비할 때 실제로 거치는 단계를 순서대로 말씀해 주세요.**
   (막히면 예를 들어 주세요 — "지난 시간 반응 확인 → 자료 찾기 → 활동지 초안 → 인쇄" 처럼)
3. **그중에서 제일 시간을 많이 잡아먹는 단계는 어디인가요?**
4. **혼자 하기 싫거나 자꾸 미루는 단계는요?**
5. **만든 자료를 누가 보나요?** (우리 반 학생 / 동교과 선생님 / 심사위원 / 연수 참가자)
6. **아직 자료가 없어서 못 하는 일이 있나요?**
   (예: 학급 반응 데이터, 지난 시험 결과, 메일 연동 — 이건 '자료 대기'로 표시됩니다)
7. **AI가 절대 하면 안 되는 일이 있다면요?** (예: 학생 이름 쓰기, 메일 보내기, 점수 매기기)

### 답을 다 받은 뒤 만들 것

**① `school.config.ts` 의 `DEPARTMENTS` 12줄**

- **id 는 절대 바꾸지 마세요.** `research, learner, design, review, write, slide, print, assess, care, comm, reflect, desk` 그대로 씁니다. 화면이 이 id로 움직입니다.
- **12개를 유지하세요.** 안 쓰는 팀은 지우지 말고, 그 선생님이 실제로 하는 다른 일로 이름을 바꿔 쓰세요.
- 바꾸는 건 `name`(팀 이름) · `icon`(이모지 1개) · `task`(그 팀이 오늘 하는 일) · `report`(끝내고 하는 말) 입니다.
- 2번에서 들은 **선생님의 실제 순서**에 팀을 맞추세요. 남의 순서를 그대로 두지 마세요.
- 3·4번에서 들은 단계에는 팀원을 한 명 더 붙이세요.

**② `TEACHER_OFFICE.md` 의 검수 기준**

- 5번 답(누가 보나)에 따라 A(공모전·보고서) / B(학생 활동지) / C(강의·연수) 중 어느 것이 주로 쓰일지 적으세요.
- 7번 답은 **안전 규칙**에 한 줄로 넣으세요. "~하지 않는다" 형태로.
- 6번 답은 `school.config.ts` 의 `PENDING` 에 넣으세요. 없는 자료를 지어내지 않고 '자료 대기'로 남기기 위한 것입니다.

**③ 바꾼 이유를 세 줄로**

무엇을 어떻게 바꿨는지, 왜 그렇게 했는지 짧게 적어주세요.
선생님이 나중에 직접 고칠 때 근거가 됩니다.

### 지킬 것

- **코드 블록 전체를 통째로 주세요.** "이 부분만 바꾸세요" 식으로 조각내지 마세요. 붙여넣기 실수가 납니다.
- 팀 이름은 **학교에서 실제로 쓰는 말**로 지으세요. 회사 말투(본부, 셀, 유닛)는 쓰지 마세요.
- `task` 와 `report` 는 한 줄씩. 두 줄이 되면 화면에서 잘립니다.
- 모르면 지어내지 말고 **되물으세요.**
- 답하는 사람에게 명령하지 마세요. "이렇게 하세요"가 아니라 "이렇게 해뒀습니다"로 적습니다.

### 끝나고 안내할 것

만든 코드를 `school.config.ts` 에 덮어쓰고 저장하면, 켜져 있는 화면이 바로 바뀝니다.
되돌리려면 `school.config.ts.bak` 의 이름에서 `.bak` 만 지우면 됩니다.
