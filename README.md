# AI 교무실

12개 AI 팀이 수업 준비를 돌리고, **선생님은 하루에 한 번만 결정합니다.**

혼자 다 하지 말고, 역할을 쪼개서 맡겨보자는 도구입니다.
설치하면 바로 돌아갑니다. **연동도, API 키도 필요 없습니다.**

---

## 1. 실행하기 (3분)

**필요한 것**: Node.js 18 이상 ([nodejs.org](https://nodejs.org)에서 설치)

```bash
npm install
npm run dev
```

브라우저에서 **http://localhost:5180** 을 엽니다.
화면 위 **"오늘 업무 시작"** 을 누르면 31명이 출근합니다.

> 윈도우 PowerShell에서는 `&&` 를 못 씁니다. 한 줄씩 따로 실행하세요.
> 창에서 `Ctrl + C` 를 누르면 서버가 꺼집니다.

---

## 2. 내 교무실로 바꾸기

**`school.config.ts` 이 파일 하나만 고치면 됩니다.**

| 고칠 것 | 어디 |
|---|---|
| 학교 이름, 화면 제목 | `SCHOOL` |
| 내 이름·호칭·담당 과목 | `TEACHER` |
| 부서 12개 이름·하는 일 | `DEPARTMENTS` |
| 직원 이름·성격·말버릇 | `STAFF` |
| 자료가 없어 멈춘 팀 | `PENDING` |
| 화면 아래 내 크레딧 | `CREDITS` |

저장하면 화면이 바로 새로고침됩니다.

### ⚠️ 딱 2가지만 지키세요

1. **부서 `id`는 바꾸지 마세요** (`research`, `design`, `review` …)
   시뮬레이션이 이 id로 움직입니다. 바꾸면 직원이 길을 잃어요.
   → 바꿔도 되는 건 `name` · `icon` · `short` · `task` · `report` 입니다.

2. **부서는 12개를 유지하세요.** 교무실 배치가 12칸 고정입니다.
   안 쓰는 부서는 지우지 말고 **이름만 바꿔서** 쓰세요.

직원 수는 자유입니다. 단 **한 팀에 팀장(`rank: "lead"`)은 1명**.

> 설정이 어긋나면 화면에 **무엇이 잘못됐는지 한국어로** 띄웁니다.
> 조용히 깨지지 않으니 안심하고 고치세요.

### 영어 교사용·중학교용으로 바꾸려면

`PROMPTS.md` 의 프롬프트를 Claude나 ChatGPT에 복붙하세요. 부서 12개를 통째로 다시 지어줍니다.

---

## 3. 하루가 이렇게 돌아갑니다

```
08:00  전원 출근
  ↓    자료조사 → 학습자 분석 → 수업 아이디어 10개 → 교육과정 검수
  ↓    TOP 3 선정
🙋 선생님  ★ 여기서 딱 한 번 결정 ★   ← AI가 진짜로 멈춥니다
  ↓    활동지 집필 → 수업자료·학습지 제작 → 평가 문항 → 관찰 기록
  ↓    가정통신 초안 → 성찰 기록
16:4x  교무 비서실이 교무실로 걸어와 브리핑
```

**⑦번이 이 구조의 심장입니다.** 멈추게 하지 않으면 AI는 끝까지 혼자 달려서
활동지까지 다 만들어버립니다. 그럼 내 수업이 아니라 AI 수업이죠.

---

## 4. 선생님 지시창

화면 오른쪽에서 직접 물어볼 수 있습니다.

| 물어보면 | 답하는 사람 |
|---|---|
| "현황 보고" | 교무 비서실장 |
| "왜 늦어져?" | 비서실장이 **진짜 병목 1개**를 짚어서 |
| "검수팀 뭐해?" | 그 팀 팀장이 팀원 현황까지 |
| "집중 모드" | 전원 자리로 복귀 |

답변마다 **출처 배지**가 붙습니다.

- `규칙` — 화면의 실제 상태를 읽어 만든 답 (즉답·무료)
- `안내` — 시스템 안내

> 이 배지가 있어서 **어디까지가 실제 상태인지** 헷갈리지 않습니다.

---

## 5. 화면 속 직원이 진짜로 일하는 건 아닙니다

솔직하게 말씀드립니다. **이 교무실은 시뮬레이션입니다.**

- 직원들이 걸어다니고 회의하는 건 **정해진 각본**입니다
- "TOP 3 확정"의 내용은 실제로 만들어진 게 아닙니다
- AI 모델을 호출하지 않습니다 (그래서 무료이고 빠릅니다)

**그럼 왜 쓰냐면** — 내 업무를 12개로 쪼개서 보면, 어디서 막히는지가 눈에 보입니다.
"학습자 분석팀이 자료 대기"라고 떠 있으면, 내가 반응 데이터를 한 번도 안 모았다는 뜻이거든요.

각본은 `src/engine/sim.ts` 맨 위 `SCRIPT` 배열입니다. 단계를 바꾸고 싶으면 거기만 고치세요.

---

## 6. 자주 막히는 곳

**`npm install` 에서 에러가 나요**
`node -v` 로 확인해서 18 미만이면 새로 설치하세요.

**5180 포트가 이미 쓰이고 있대요**
`npm run dev -- --port 5181`

**직원이 안 움직여요**
위쪽 **"오늘 업무 시작"** 을 눌러야 하루가 시작됩니다.

**부서를 지웠더니 화면이 안 떠요**
12개를 유지해야 합니다. 화면에 뜨는 안내대로 되돌리고 이름만 바꾸세요.

**글자가 깨져요**
윈도우 기본 한글 글꼴(맑은 고딕)을 씁니다. 맥에서는 알아서 비슷한 글꼴로 바뀝니다.

---

## 7. 오늘 당장 할 1개

12개 다 고치려 하지 마세요. 오늘은 이것만요.

`school.config.ts` 를 열고 **`TEACHER.name` 을 내 이름으로** 바꾸세요.
그다음 **부서 하나**만 우리 학교 말로 고치고요.

내일 아침에 열어서 내 이름이 교무실에 앉아 있으면, 그때 나머지를 고치게 됩니다.

---

## 만든 것에 대해

이 프로그램은 **자유롭게 쓰고 고치고 나눠도 됩니다.** `LICENSE.md` 를 보세요.
`school.config.ts` 의 `CREDITS` 에 이름을 넣으면 화면 아래에 표시됩니다.
